# Specky

A simple spectrum analyzer.

# Development

Install module into environment 

    pip install -e .[dev]

Run via

    import spectran
    spectran.run()

or in terminal 

    python -m spectran